server:
  port: 8081
spring:          
  application:
    name: SpringSecurityDemo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: PhuongTay110903@
    url: jdbc:mysql://localhost:3306/spring_security_demo?useSSL=false&useUnicode=true&characterEncoding=UTF-8
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
